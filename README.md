
## Start kafka
Should be installed Docker.

Install node modules:

- ```yarn```

Start kafka
- ```yarn kafka```

Start server
- ```yarn watch```

Open in browser:
- ```http://localhost:5001```

Start generate events from events.csv
- ```yarn sendEvents```
